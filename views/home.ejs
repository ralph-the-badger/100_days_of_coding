<%- include("./includes/head.ejs") %>
  </head>
  <body>
    <main class="main-area">
      <%- include("./includes/navigation.ejs") %>
      <% if (perso.length > 0) { %>
        <% perso.forEach(item => { %>
          <h2>Hallo, <%= item.name  %></h2>
        <% }); %>  
      <% } else { %>     
        <h2>Familienrezepte</h2>
      <% } %> 
    </main>
  </body>
</html>
