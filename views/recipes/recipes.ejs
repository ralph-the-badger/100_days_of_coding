<%- include("../includes/head.ejs") %>
</head>

<body>
  <main class="main-area">
    <%- include("../includes/navigation.ejs") %>
    <% if (rec.length > 0) { %>
    <% rec.forEach(item => { %>
    <h2><%= item.recipeName  %></h2>
    <h3><%= item.recipeImage  %></h3>
    <h3><%= item.recipeIngredients  %></h3>
    <h3><%= item.recipeDescription  %></h3>
    <h3><%= item.recipeInfo  %></h3>
    <% }); %>
    <% } else { %>
    <h2>Familienrezepte</h2>
    <% } %>
  </main>
</body>

</html>